<%= form_for @topic do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.collection_check_boxes :category_ids, Category.all, :id, :name %>

  <%= f.label :content %>
  <%= f.text_area :content %>

  <%= f.submit "Submit" %>

<% end %>